# Nginx 설정 파일에 Content Security Policy 설정 추가
server {
    # 다른 설정들...

    # Content Security Policy 설정
    add_header Content-Security-Policy "default-src 'self' *; style-src 'self' 'unsafe-inline' *; script-src 'self' 'unsafe-inline' *; img-src 'self' * data:;";
    
    # 다른 설정들...
}
